# OraProjekt — GitLab CI/CD Pipeline (Minimal)
# Full pipeline will be added after verifying basic setup works

stages:
  - test

lint:
  stage: test
  image: oven/bun:latest
  script:
    - bun install
    - bun run lint
  rules:
    - if: $CI_COMMIT_BRANCH
