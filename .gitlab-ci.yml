# OraProjekt — GitLab CI/CD Pipeline
# Minimal test pipeline

stages:
  - test

hello:
  stage: test
  script:
    - echo "OraProjekt CI/CD pipeline is working!"
    - echo "Branch: $CI_COMMIT_BRANCH"
    - echo "Commit: $CI_COMMIT_SHA"
  rules:
    - if: $CI_COMMIT_BRANCH
